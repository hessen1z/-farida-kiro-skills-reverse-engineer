C++ SDK Loader (placeholder)

Design: Provide a lightweight C++ header + implementation that can parse the `meta/` JSON files and offer a `Library` API similar to the Python loader.

Status: Stub — implement as needed.
