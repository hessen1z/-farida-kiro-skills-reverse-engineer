TypeScript SDK Loader (placeholder)

Design: Provide an npm package scaffold that exposes `Library.load(skillName)` and `Library.findPattern(name)` using the JSON files under `meta/`.

Status: Stub — implement as needed.
